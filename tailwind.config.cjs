const config = {
  content: ["./src/**/*.{html,js,svelte,ts}"],

  theme: {
    extend: {
      backgroundImage: {
        'cust-background': "url('../img/487.png')",
      }
    },
  },

  plugins: [],
};

module.exports = config;
